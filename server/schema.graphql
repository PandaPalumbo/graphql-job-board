type Query {
#    [] signifies an array of given type
    jobs: [Job],
    companies: [Company]
}

type Company {
    id: ID!
    name: String
    description: String
}
type Job {
#    ! denotes cannot be null
    id: ID!
    title: String
    description: String,
    company: Company
}

